import React from 'react';
import Paper from '../Paper';
import { Typography } from '@material-ui/core';

const LoginPage = () => {
  const content = <Typography>LoginPage</Typography>;

  return <Paper content={content}></Paper>;
};

export default LoginPage;
